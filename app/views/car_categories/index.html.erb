<h1>Categorias de Carros</h1>
<h3><%= link_to 'Registrar nova Categoria', new_car_category_path%> </h3>
<ul>
<% @categorias.each do |c| %>
    <li>
    <%= link_to c.name, car_category_path(c) %>
    </li>
<% end %>
</ul>